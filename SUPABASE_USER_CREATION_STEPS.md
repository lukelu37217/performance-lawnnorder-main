# 🖼️ Supabase 用户创建 - 详细步骤

## 完整路径导航

### 第一步: 登录 Supabase Dashboard

```
1. 打开浏览器
2. 输入: https://supabase.com/dashboard
3. 使用你的账户登录
4. 在项目列表中找到: "Performance Evaluation System"
5. 点击进入
```

---

### 第二步: 点击 Authentication

```
Left Sidebar (左侧菜单):
┌─────────────────────┐
│ Home                │
│ SQL Editor          │
│ Table Editor        │
│ Authentication  ← 点击这里
│ Storage             │
│ Realtime            │
│ Functions           │
│ Edge Functions      │
│ Extensions          │
└─────────────────────┘
```

---

### 第三步: 进入 Users 管理

```
Authentication 页面:
┌──────────────────────────────────┐
│ Authentication                   │
├──────────────────────────────────┤
│                                  │
│ MANAGE                           │
│ ├─ Users            ← 点击这里    │
│ │                                │
│ CONFIGURATION                    │
│ ├─ Providers                     │
│ ├─ Policies                      │
│ └─ ...                           │
│                                  │
└──────────────────────────────────┘
```

---

### 第四步: 点击 Add User 按钮

```
Users 页面:
┌──────────────────────────────────────────────────┐
│ Users                    [Add user] ← 点击这个     │
├──────────────────────────────────────────────────┤
│                                                  │
│ UID              Email              Created     │
│ ─────────────────────────────────────────────   │
│ (可能是空的或有现有用户)                         │
│                                                  │
└──────────────────────────────────────────────────┘
```

---

### 第五步: 填写用户表单

```
弹出的表单应该看起来像:

┌─────────────────────────────────────┐
│  Create a new user                  │
├─────────────────────────────────────┤
│                                     │
│  Email *                            │
│  ┌──────────────────────────────┐  │
│  │ dylan@lawnorder.ca           │  │ ← 输入这个
│  └──────────────────────────────┘  │
│                                     │
│  Password *                         │
│  ┌──────────────────────────────┐  │
│  │ dylan1234                    │  │ ← 输入这个
│  └──────────────────────────────┘  │
│                                     │
│  ☑ Auto Confirm user               │ ← 确保勾选
│                                     │
│  [Save]          [Cancel]           │ ← 点击 Save
│                                     │
└─────────────────────────────────────┘
```

**关键字段:**
- Email: `dylan@lawnorder.ca`
- Password: `dylan1234`
- Auto Confirm user: ✅ **必须勾选**

---

### 第六步: 验证成功

```
成功后应该看到:

┌────────────────────────────────────────┐
│ ✓ User created successfully            │ ← 绿色提示
└────────────────────────────────────────┘

用户列表现在显示:

UID                          Email                  Created
─────────────────────────────────────────────────────────
072fd764-7440-4793...       dylan@lawnorder.ca     Today
```

---

## 如果看不到 "Add user" 按钮

### 可能原因 1: 权限不足

```
检查:
1. 你是否是项目的 Owner 或 Admin?
2. 在 Project Settings > Members 中检查你的角色
```

### 可能原因 2: 还没有配置认证

```
解决方案:
1. 确保 Supabase 项目已完全初始化
2. 尝试刷新页面
3. 或者使用 SQL 插入用户（见下方）
```

### 备选方案: 使用 SQL 创建用户

如果 UI 不可用，在 SQL Editor 中运行:

```sql
-- 1. 首先检查是否需要 auth 扩展
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- 2. 直接插入到 auth.users 表
INSERT INTO auth.users (
  instance_id,
  id,
  aud,
  role,
  email,
  encrypted_password,
  email_confirmed_at,
  invited_at,
  confirmation_token,
  confirmation_sent_at,
  recovery_token,
  recovery_sent_at,
  email_change_token,
  email_change_sent_at,
  last_sign_in_at,
  raw_app_meta_data,
  raw_user_meta_data,
  is_super_admin,
  created_at,
  updated_at,
  phone,
  phone_confirmed_at,
  phone_change_token,
  phone_change_sent_at
) VALUES (
  '00000000-0000-0000-0000-000000000000'::uuid,
  gen_random_uuid(),
  'authenticated',
  'authenticated',
  'dylan@lawnorder.ca',
  crypt('dylan1234', gen_salt('bf')),
  NOW(),
  NULL,
  '',
  NULL,
  '',
  NULL,
  '',
  NULL,
  NOW(),
  '{"provider":"email","providers":["email"]}',
  '{}',
  false,
  NOW(),
  NOW(),
  NULL,
  NULL,
  '',
  NULL
) ON CONFLICT DO NOTHING;
```

**注意:** SQL 方法不推荐，最好用 UI。

---

## 完成后做什么

用户创建后:

### 1️⃣ 运行数据 SQL 脚本

File: `setup-real-data.sql`

在 SQL Editor 中运行此脚本来创建:
- 组织结构 (Leaders, Foremen, Workers)
- 人员关系
- 链接 dylan 用户

### 2️⃣ 清空浏览器缓存

```
快捷键: Ctrl + Shift + Delete
选择: 全部内容
点击: Clear data
```

### 3️⃣ 硬刷新网页

```
快捷键: Ctrl + Shift + R
或者: F12 → 右键刷新按钮 → "Hard refresh"
```

### 4️⃣ 测试登录

```
网址: https://lawnorder-performance.vercel.app/
邮箱: dylan@lawnorder.ca
密码: dylan1234
```

---

## 故障排除

### 问题: "Email already exists"

```
原因: 这个邮箱已经被创建过了

解决:
1. 使用不同的邮箱 (如 dylan2@lawnorder.ca)
2. 或者删除现有用户再创建
3. 或者重置现有用户的密码
```

### 问题: 创建后无法登录

```
原因: 可能是:
1. 邮箱/密码不对
2. 用户未被 "Auto Confirm"
3. 缓存问题

解决:
1. 确认邮箱和密码完全一致
2. 删除用户，重新勾选 "Auto Confirm user"
3. 清空浏览器缓存 (Ctrl+Shift+Delete)
4. 硬刷新 (Ctrl+Shift+R)
```

### 问题: 登录后 Dashboard 空白

```
原因: SQL 数据脚本没有运行

解决:
1. 运行 setup-real-data.sql
2. 确认 SQL 脚本执行成功
3. 刷新页面
```

---

## 快速检查清单

创建用户前，确认:

- [ ] 你登录了 Supabase Dashboard
- [ ] 在正确的项目: "Performance Evaluation System"
- [ ] 找到了 Authentication > Users
- [ ] 看到了 "Add user" 按钮

创建用户后，确认:

- [ ] 用户出现在用户列表中
- [ ] Email 是 dylan@lawnorder.ca
- [ ] Created 日期是今天
- [ ] 可以在 Users 列表中看到该用户

运行 SQL 后，确认:

- [ ] SQL 脚本执行成功 (没有错误)
- [ ] leaders 表有 1 行 (Brian)
- [ ] foremen 表有 5 行 (Max, Dusty, Rana, Lucas, Daria)
- [ ] workers 表有 15 行
- [ ] profiles 中的 dylan 有正确的 entity_id

---

## 最终流程

```
┌─ 创建 Supabase Auth 用户 (5分钟)
│  ├─ dylan@lawnorder.ca / dylan1234
│  └─ Auto Confirm: ✅
│
├─ 运行 setup-real-data.sql (3分钟)
│  ├─ 创建组织结构
│  └─ 链接用户
│
├─ 清空缓存和硬刷新 (2分钟)
│  ├─ Ctrl+Shift+Delete
│  └─ Ctrl+Shift+R
│
└─ 测试登录 (2分钟) → 🎉 完成！
```

**总时间: 12 分钟**

---

**现在就开始:** 按上面的步骤在 Supabase 中创建用户，然后运行 SQL 脚本！

